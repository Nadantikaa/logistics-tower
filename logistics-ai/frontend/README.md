# Logistics AI Frontend

Run locally from `frontend/`:

```powershell
npm install
npm run dev
```

If your FastAPI backend is not on `http://127.0.0.1:8000`, set:

```powershell
$env:VITE_API_BASE="http://127.0.0.1:8000/api"
```
